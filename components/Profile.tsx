import React from "react";
import { ProfileProps } from "@types";

const Profile = ({
  name,
  desc,
  data,
  handleDelete,
  handleEdit,
}: ProfileProps) => {
  return <div>Profile</div>;
};

export default Profile;
